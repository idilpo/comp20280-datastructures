import java.util.Iterator;
import java.util.Random;
/*
public class p1q7 {

    public class myArray<T> implements Iterable<T> {
        private T [] x;

        myArray(T [] input) {
            x = new int[input.length];
            // copy input in x
        }

        public Iterator<T> iterator() {return new MyIterator<T>();}

        class MyIterator<T> implements Iterator<T> {
            public boolean hasNext() {
                // implement
            }

            public T next() {
                // implement
            }

            // remove
        }

        public static void main(String [] args) {
            Random rand = new Random();
            Integer[] arr = rand.ints(10, 25, 50).boxed();
            myArray<Integer> arr = new myArray<Integer>(arr);
            for (Integer i : arr) {System.out.println(i);}
        }

    }
    
 */

}
